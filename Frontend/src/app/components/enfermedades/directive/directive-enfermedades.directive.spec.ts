import { DirectiveEnfermedadesDirective } from './directive-enfermedades.directive';

describe('DirectiveEnfermedadesDirective', () => {
  it('should create an instance', () => {
    const directive = new DirectiveEnfermedadesDirective();
    expect(directive).toBeTruthy();
  });
});
