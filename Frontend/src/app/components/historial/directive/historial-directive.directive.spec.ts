import { HistorialDirectiveDirective } from './historial-directive.directive';

describe('HistorialDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new HistorialDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
