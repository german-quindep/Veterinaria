import { DirectiveVeterinarioDirective } from './directive-veterinario.directive';

describe('DirectiveVeterinarioDirective', () => {
  it('should create an instance', () => {
    const directive = new DirectiveVeterinarioDirective();
    expect(directive).toBeTruthy();
  });
});
