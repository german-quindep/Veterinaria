import { DirectiveVacunaDirective } from './directive-vacuna.directive';

describe('DirectiveVacunaDirective', () => {
  it('should create an instance', () => {
    const directive = new DirectiveVacunaDirective();
    expect(directive).toBeTruthy();
  });
});
